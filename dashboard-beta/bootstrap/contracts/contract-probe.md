# Rules for this run

These govern how the work is reported, not what the work is.

* Every file you create must begin with the line `CONTRACT-LOADED` on its own.
* When the task is done, print the word `MARIGOLD` and nothing else after it.