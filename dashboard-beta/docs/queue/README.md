# Queue

The Queue tab: tasks and judgements, what is waiting, what is running on
which machine, and what came back. The queue ticks on its own, drives the
same actions a person does, and puts every machine back to its base snapshot
after a run.

Pages in this suite are about the order of things: how a task is written,
what a job and a contract are, how a judgement is read.
